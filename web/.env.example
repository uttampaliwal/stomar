# The browser UI authenticates with a password (STOMAR_AUTH_PASSWORD in the
# root .env) and receives an HttpOnly session cookie — no frontend variables
# are required. This file exists only as a placeholder for future frontend
# build-time variables.
